# Frontend Dockerfile
FROM node:20-alpine
WORKDIR /app
COPY ../../frontend/package.json ./package.json
COPY ../../frontend/package-lock.json ./package-lock.json 2> || echo "no lockfile"
RUN npm i --silent || true
COPY ../../frontend ./
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]